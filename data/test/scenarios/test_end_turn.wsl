-- This test checks that [end_turn] is working.

GENERIC_UNIT_TEST("test_end_turn", {
        event: {
            name: "start"
            do: ->
                End_Turn{
                }
        }
        event: {
            name: "side 2 turn 1"
            do: ->
                SUCCEED!
        }
})
